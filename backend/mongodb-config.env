# MongoDB Atlas Configuration
# Substitua <db_username> e <db_password> pelas suas credenciais reais

MONGODB_URI=mongodb+srv://<db_username>:<db_password>@cluster0.wyktcyy.mongodb.net/sos-voz-feminina?retryWrites=true&w=majority&appName=Cluster0

# Super Admin Credenciais
SUPER_ADMIN_EMAIL=sosvozfeminina@administrador.com
SUPER_ADMIN_PASSWORD=@VozAdministrador!25

# Para usar, copie este arquivo para .env e substitua as credenciais:
# cp mongodb-config.env .env
# Depois edite o arquivo .env com suas credenciais reais
